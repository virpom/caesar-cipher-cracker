# 🔐 Caesar Cipher Cracker — C++ Edition

Полный порт Python-версии на C++17. Без внешних зависимостей — только стандартная библиотека.

## Сборка

```bash
cd cpp
make
```

Или напрямую:

```bash
c++ -std=c++17 -O2 -o caesar caesar.cpp
```

Требования: компилятор с поддержкой C++17 (`g++`, `clang++`).

## Использование

```bash
# Аргументом
./caesar "Фхнжйч снх пеп ийре"

# Через pipe
echo "Khoor zruog" | ./caesar

# Только расшифрованный текст (для pipe/скриптов)
./caesar -r "зашифрованный текст"

# Двуязычный текст (RU + EN с разными ключами)
./caesar "Фхнжйч снх Khoor zruog"

# Принудительный язык
./caesar -l en "Khoor zruog"

# Принудительная проверка смешанного шифра
./caesar -m "текст"

# Интерактивный режим (многострочный ввод)
./caesar
```

## Флаги

- `-r`, `--raw` — только расшифрованный текст, без оформления
- `-m`, `--mixed` — принудительно проверить смешанный шифр
- `-l`, `--lang ru|en` — принудительно задать язык
- `-h`, `--help` — справка

## Возможности

Идентичны Python-версии:

- **6 уровней анализа**: Chi², биграммы, IC, словарь, стемминг, адаптивные веса
- **Двуязычность**: автодетект RU+EN, раздельная дешифровка каждого сегмента
- **Смешанные шифры**: скользящее окно для обнаружения смены ключа
- **ANSI-цвета**: цветной вывод в терминале (автоотключение в pipe/--raw)
- **~7 млн слов** из словарей (используются общие словари из корня проекта)

## Словари

Программа ищет `russian_dict.txt` и `english_dict.txt` в нескольких местах:

1. Директория исполняемого файла (`cpp/`)
2. Родительская директория (корень проекта — основной путь)
3. Текущая рабочая директория
4. Домашняя директория (`$HOME`)

## Оптимизации по сравнению с первой версией

- **Один `utf8_decode` на сдвиг** — предвычисленные кодпоинты передаются всем скорерам
- **O(1) поиск биграмм** — плоский bool-массив вместо хеш-таблицы
- **`reserve` в UTF-8 кодировщике** — меньше реаллокаций
- **`stem_dict_score` работает с кодпоинтами** — без decode→pop→encode в цикле
- **Безопасный вывод** — `std::string` вместо `snprintf` с фиксированным буфером

## Структура кода

```
caesar.cpp (~1160 строк)
├── УТИЛИТЫ UTF-8          — декодирование/кодирование кодпоинтов
├── КЛАССИФИКАЦИЯ СИМВОЛОВ  — is_ru/is_en, индексы алфавита, регистр
├── ANSI-ЦВЕТА             — цветной вывод в терминале
├── КОНСТАНТЫ              — частоты букв (НКРЯ/Cornell), биграммы, суффиксы
├── СТРУКТУРЫ ДАННЫХ       — ShiftResult, Segment, LangSegment
├── СЛОВАРЬ                — ленивая загрузка, поиск файлов
├── ДЕШИФРОВЩИК            — сдвиг букв указанного языка
├── СКОРЕРЫ                — chi², биграммы, IC, словарь, стемминг
├── АНАЛИЗАТОР             — адаптивная комбинация, crack(), is_plaintext()
├── РАЗБИЕНИЕ ПО ЯЗЫКАМ   — split_by_language()
├── ДЕТЕКТОР СМЕШАННЫХ     — скользящее окно, сглаживание, слияние
└── CLI / MAIN             — аргументы, вывод, главная логика
```
