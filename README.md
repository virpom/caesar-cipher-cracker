# 🔐 Caesar Cipher Cracker — Ultimate Edition

Автоматический взломщик шифра Цезаря с многоуровневым анализом.  
Поддерживает **русский** и **английский** языки, смешанные шифры, Rich TUI.

## Возможности

- **6 уровней анализа**: Chi², биграммы, Index of Coincidence, словарь, стемминг, адаптивные веса
- **~7 млн слов** в словарях (2.2M русских + 4.6M английских из Wiktionary)
- **Автоопределение языка** (по количеству RU/EN символов)
- **Смешанные шифры** — детектирует текст, зашифрованный несколькими ключами
- **Rich TUI** с цветными таблицами и панелями (fallback на plain text)
- **Режим `--raw`** — выводит только расшифрованный текст (удобно для pipe и скриптов)
- **Определение открытого текста** — не тратит время если текст не зашифрован

## Установка

```bash
git clone https://github.com/virpom/caesar-cipher-cracker.git
cd caesar-cipher-cracker
python3 caesar.py "зашифрованный текст"
```

Словари уже в репозитории — ничего дополнительно скачивать не нужно.

Опционально: `pip install rich` для красивого TUI (работает и без него).

## Использование

### Дешифровка

```bash
# Аргументом
python3 caesar.py "зашифрованный текст"

# Через pipe
echo "зашифрованный текст" | python3 caesar.py

# Интерактивный режим
python3 caesar.py
```

### Режим --raw (только текст)

```bash
# Только расшифрованный текст — легко копировать
python3 caesar.py --raw "Aol xbpjr iyvdu mve qbtwz vcly aol shgf kvn"
# → The quick brown fox jumps over the lazy dog

# Использование в pipe
echo "зашифрованный текст" | python3 caesar.py -r | pbcopy
```

### Флаги

| Флаг | Описание |
|------|----------|
| `-r`, `--raw` | Вывести только расшифрованный текст |
| `-m`, `--mixed` | Принудительно проверить смешанный шифр |
| `-l ru/en`, `--lang` | Принудительно задать язык (иначе авто) |
| `-h`, `--help` | Справка |

### Шифрование (для тестов)

```bash
python3 caesar_encode.py 7 "The quick brown fox"
python3 caesar_encode.py 5 "привет мир"
```

## Как работает

### Многоуровневый анализ

Для каждого возможного ключа (33 для RU, 26 для EN) вычисляются:

1. **Chi-squared** — сравнение частот букв с эталоном (НКРЯ / Cornell)
2. **Биграммный анализ** — доля распознанных пар букв (80 частых биграмм)
3. **Словарный поиск** — 4 уровня: точное → без ё → стемминг → стемминг+ё
4. **Агрессивный стемминг** — прогрессивная обрезка до нахождения корня
5. **Index of Coincidence** — статистическая мера (RU≈0.055, EN≈0.067)
6. **Адаптивные веса** — комбинация метрик меняется в зависимости от длины текста

### Адаптивные веса

| Длина текста | Chi² | Биграммы | Словарь | Стемминг |
|-------------|------|----------|---------|----------|
| ≥100 букв | 35% | 10% | 35% | 20% |
| 30-99 | 20% | 20% | 35% | 25% |
| 10-29 | 10% | 30% | 35% | 25% |
| <10 | 5% | 45% | 30% | 20% |

### Смешанные шифры

Если уверенность единого ключа < 60%, запускается детектор смешанных шифров:
- Скользящее окно (40 символов) определяет оптимальный ключ для каждой позиции
- Сглаживание голосованием большинства (окно 15)
- Границы сегментов привязываются к пробелам/знакам препинания

## Точность

| Тип текста | Точность |
|-----------|---------|
| Длинный (≥10 слов) | ~95% |
| Средний (5-10 слов) | ~85% |
| Короткий (2-4 слова) | ~70% |

Протестировано на стихотворении Пушкина (ключ 29) — 77/87 слов, 82.2% уверенность.

## Структура проекта

```
caesar-cipher-cracker/
├── caesar.py           # Главное приложение (~970 строк)
├── caesar_encode.py    # Утилита шифрования (для тестов)
├── download_dicts.sh   # Скрипт скачивания словарей
├── russian_dict.txt    # Словарь RU (2.2M слов, .gitignore)
├── english_dict.txt    # Словарь EN (4.6M слов, .gitignore)
├── README.md           # Этот файл
└── ARCHITECTURE.md     # Техническая архитектура
```

## Требования

- Python 3.8+
- Стандартная библиотека (без внешних зависимостей)
- `rich` — опционально, для красивого TUI

## Лицензия

MIT
